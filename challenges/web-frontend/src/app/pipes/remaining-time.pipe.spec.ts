import { RemainingTimePipe } from './remaining-time.pipe';

describe('RemainingTimePipe', () => {
  it('should return updated value each seconds', () => {
  });

  it('should return "null" if there is no value', () => {
  });

  it('should return "Ended" if the remaining time is 0', () => {
  });
});
